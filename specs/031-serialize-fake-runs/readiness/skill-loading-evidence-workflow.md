# Skill Loading Evidence Workflow

Status: complete for focused guidance implementation.

Each task with a non-empty `skillist` records the task id, skill id, resolved
path, load result, `loaded_at`, `work_started_at`, evidence path, and reviewer
exception fields before completion.

Evidence path: `readiness/skill-loading-evidence.md`.
