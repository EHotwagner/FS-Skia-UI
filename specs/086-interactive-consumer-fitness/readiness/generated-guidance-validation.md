# Generated-guidance validation — feature 086

The neutral controls-first scaffold (US1) and the controls-family pointer-host default (US2)
change generated guidance/source. Validation obligations:

- authoritative command: `./fake.sh build -t GeneratedGuidanceCheck` (+ TemplateCheck,
  GeneratedProductCheck for the generated source).
- artifact path: generated `docs/`/`Selected-Controls` guidance + product source under the
  generated tree.
- failure class: a dropped durable governance-scanned token (`--scene-evidence`,
  `SceneEvidence.render`, `RendererMode = "deterministic-scene"`, the visual-evidence honesty
  vocabulary, `diagnostic-class=*` window facts) is a **blocking** generated-guidance failure.
- next action: re-point the neutral source while preserving every must-survive token
  (scaffold-map.md § Must-survive); the `fs-skia-viewer-host` warm-up note regenerates to
  `.claude` via `RefreshSurfaceBaselines`.

Status: pending — US1/US2 generated-source changes not yet landed; framework `.fsi`/behavior
changes validated via package tests (`Dev` scope).
