# Capability Loading Workflow

Status: placeholder created during setup.

Required final evidence:

- Skill-loading records in `readiness/skill-loading-evidence.md`
- For every non-empty task skill list: task id, skill id, resolved path, load result, `loaded_at`, `work_started_at`, evidence path, reviewer exception
- Confirmation that valid empty skill lists were intentionally empty
