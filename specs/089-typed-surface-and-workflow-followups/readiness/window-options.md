# Window Options (089)

status=not-applicable

## Option rows

- option=resize — resize=not-applicable (no window created)
- option=maximize — maximize=not-applicable (no window created)
- option=startup-state — startup-state=not-applicable (no window created)
- option=startup-position — startup-position=not-applicable (no window created)
- option=backend — backend=not-applicable (no windowed backend selected; this
  governance/docs feature uses no window)

No unsupported window setting is requested, and no option failure is hidden under
another diagnostic class. This feature opens no window, so there are no window
options to honor or degrade.
