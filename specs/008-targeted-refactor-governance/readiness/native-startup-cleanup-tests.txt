Command:

```bash
dotnet test tests/Lib.Tests/Lib.Tests.fsproj --no-build
```

Result: PASS.

Focused evidence:

- `Native startup cleanup.startup stage inventory covers every owned Vulkan and Skia resource category`
- `Native startup cleanup.injected acquisition failures Synthetic release acquired resources once in reverse order`
- `Native startup cleanup.successful shutdown Synthetic releases all acquired resources once and repeated cleanup is idempotent`

Synthetic disclosure: the deterministic startup failure cases use symbolic
resource handles to force each native failure stage. Real Vulkan smoke evidence
is recorded in `native-smoke.txt`.
