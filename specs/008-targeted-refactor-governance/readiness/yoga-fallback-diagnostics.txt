Focused command:

```bash
dotnet test tests/Layout.Tests/Layout.Tests.fsproj --no-restore
```

Result: PASS.

Evidence:

- `tests/Layout.Tests/YogaFallbackDiagnosticsTests.fs`
- `specs/008-targeted-refactor-governance/readiness/logs/layout-tests.txt`

Verdict: recoverable Yoga execution failure keeps safe deterministic bounds and
emits a `FallbackBoundsApplied` warning with `Constraint = Some "yoga"` and
`FallbackApplied = true` through the existing public `LayoutDiagnostic` fields.
