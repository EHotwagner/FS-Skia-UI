# Template Drift Fixtures

The `missing-alignment` and `invalid-deferral` fixtures are implemented as
script-level fixtures in `scripts/template-drift.fsx` so tests can exercise the
same command-line validator used by the FAKE target.
