# Generated-Consumer Currency (US4, FR-010, SC-007)

After the single-sourcing regeneration, generated consumers stay byte-correct: the
`.agents`↔`.claude` skill peers, the template-owned files, and a freshly generated
`dotnet new fs-skia-ui` project all carry correct, non-stale governance content.

## Confirmation (T015) — all green, 2026-06-03

| Gate | Result | What it proves |
| --- | --- | --- |
| `./fake.sh build -t SkillSyncCheck` | `Status: Ok` | the generated `.claude/skills` tree is a current, byte-identical regeneration of the canonical `.agents/skills` source across all 26 enumerated files (the `gov/*` regions spliced into `fs-skia-layout-evidence` propagated to its `.claude` peer; the echo-removed `speckit-tasks` peer is current) — see `skill-sync-check.md` |
| `./fake.sh build -t TemplateDrift` | `Status: Ok` | every changed template-owned path resolves its required alignment classes; no drift blockers — see `template-drift.md` |
| `./fake.sh build -t TemplateCheck` | `Status: Ok` | the `dotnet new fs-skia-ui` template instantiates and validates against the regenerated governance corpus |
| `./fake.sh build -t GeneratedProductCheck` | `Status: Ok` | a generated product receives correct, non-stale governance guidance (constitution + skill set) after regeneration |

`TemplateCheck` and `GeneratedProductCheck` actually instantiate and validate the
generated product (≈2 min and ≈1.5 min respectively), so a generated project's
`constitution.md` and skill set match the regenerated canonical sources. No copy is
hand-synced — the constitution twins and `.claude` peers are regenerated by
`RefreshSurfaceBaselines` and currency-checked, satisfying FR-010 / SC-007.

See `template-drift.md` and `skill-sync-check.md` for the full gate reports.
