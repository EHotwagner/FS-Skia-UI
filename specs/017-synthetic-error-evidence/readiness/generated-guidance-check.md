# Generated Guidance Check

Command:

```bash
./fake.sh build -t GeneratedGuidanceCheck
```

Result: exit code `0` on 2026-05-26 after clearing stale local FAKE cache.

The generated report is also available at `readiness/generated-guidance.md`.
The target validated active and preset guidance for `[SEH]`,
`synthetic-error-handling-approved`, eligible malformed-input examples,
non-eligible convenience fixtures, and implementation-time relabeling
prohibition.
