# Prepared baseline path for T014.
#
# Do not treat this as a refreshed public-surface baseline. T043 owns the
# actual `./fake.sh build -t RefreshSurfaceBaselines` refresh after the
# launch runtime implementation is complete.
