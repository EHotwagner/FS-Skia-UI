# FSI session — feature 090 (T005)
# Exercising Control.nearestAuthored + ControlsElmish.routeInteractivePointer — the
# representative paths the live host (runInteractiveApp) wires (research D6).
# Captured 2026-06-10T09:09:02Z via dotnet fsi over the built package DLLs.

$ dotnet fsi capture-fsi-090.fsx
nearestAuthored rendered "0.0"     = Some "panel"  (inner positional node -> authored container)
nearestAuthored rendered "panel"   = Some "panel"  (container key -> itself, fixed point)
nearestAuthored rendered "absent"  = None  (no keyed ancestor -> None, FR-004a)
routeInteractivePointer press+release inside container-keyed panel -> [Increment]  (authored binding fired; no MapPointer clause)
