# Audit Diagnostics Fixtures

SYNTHETIC FIXTURE: these malformed readiness packages are design-approved error-handling inputs for T009. They are not success evidence.

| Fixture | Purpose |
|---------|---------|
| `missing-files` | Required readiness files are absent. |
| `incomplete-terms` | Required readiness files exist but omit audit-required terms. |
