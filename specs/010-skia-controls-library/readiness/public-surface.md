# Public Surface

## Verdict

PASS for in-repo public surface evidence.

## Evidence

- `src/Controls/Controls.fsproj` builds `FS.Skia.UI.Controls` for `net10.0`.
- Signature files exist for `Types`, `Control`, `Attributes`, `Theme`,
  `Accessibility`, `Diagnostics`, `Catalog`, `TextInput`, `Collections`,
  `Charts`, and `CustomControl`.
- `readiness/surface-baselines/FS.Skia.UI.Controls.txt` was generated by
  `dotnet fsi scripts/refresh-surface-baselines.fsx`.
- `./fake.sh build -t PackageSurfaceCheck` passed and recorded
  `readiness/package-surfaces/index.md`.
- `./fake.sh build -t FsiTranscripts` passed and recorded
  `readiness/fsi/controls-prelude.txt`.

## FSI Exercise

`scripts/controls-prelude.fsx` constructed a representative view through the
public `Control<'msg>` surface, rendered it, counted catalog rows, and dispatched
a current-view text event:

- `controls-node-count=4`
- `controls-diagnostics=[]`
- `controls-catalog-count=46`
- `controls-text-dispatch=[NameChanged "Grace"]`

## Surface Change Summary

Added the Controls package and public modules. Chart and graph widgets are now
represented through `FS.Skia.UI.Controls.Charts` modules and catalog rows; the
separate Charts capability remains compatibility source only and is no longer
active in generated capability selection.
