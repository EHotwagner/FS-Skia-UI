# FSI session — exercising the 046 draft .fsi surfaces (T006)
# Command: dotnet fsi (referencing build/Governance/bin/Debug/net10.0/FS.Skia.UI.Build.dll)

=== classifyConstitutionCheck over a small literal complete plan ===
complete -> TemplateRecognized
  [({ Id = "template-ownership"
      DisplayName = "Template ownership" }, Filled);
   ({ Id = "dependency-impact"
      DisplayName = "Dependency impact" }, Filled);
   ({ Id = "command-surface"
      DisplayName = "Command-surface impact" }, Filled);
   ({ Id = "generated-project"
      DisplayName = "Generated project impact" }, Filled);
   ({ Id = "evidence-paths"
      DisplayName = "Evidence paths" }, Filled);
   ({ Id = "fsi-contract"
      DisplayName = "`.fsi` / contract impact" }, Filled);
   ({ Id = "mvu-boundary"
      DisplayName = "MVU/effect boundary" }, Filled);
   ({ Id = "synthetic-evidence"
      DisplayName = "Synthetic evidence" }, Filled);
   ({ Id = "test-evidence"
      DisplayName = "Test evidence" }, Filled);
   ({ Id = "observability"
      DisplayName = "Observability" }, Filled);
   ({ Id = "deferred-scope"
      DisplayName = "Deferred scope" }, Filled)]
complete findings -> []

=== classifyConstitutionCheck over an incomplete plan (one area blanked) ===
incomplete findings -> [{ ArtifactClass = "constitution-check"
   Path = "plan.md"
   Rule = "deferred-scope"
   Message = "Deferred scope is empty or absent" }]

=== classifyViolation over a literal contract with a Required and a Deprecated rule ===
violate Required 'req' -> Fail
violate Deprecated 'dep' (window open) -> Warn "rule `dep` is deprecated and will be removed in schema_version 2.0"
changelog consistency (current shipped) -> []

=== renderContractHeader current ===
schema_version: 1.0

Generated-product structural rule lifecycle:
- `product-app-present`: required — exactly one product app project
- `product-test-suite-present`: required — exactly one product test suite
- `required-files-present`: required — required generated files present
- `api-surface-bundled`: required — bundled docs/api-surface stays in lockstep with source
- `effects-boundary-doc`: required — self-contained docs/effects-boundary.md
- `no-demo-identifiers`: required — generated starter carries no demo identifiers
- `consumer-facing-skills`: required — generated capability skills are consumer-facing
- `widgets-skill-present`: required — generated app carries the widgets skill
- `no-stale-charts`: required — no stale Charts package or skill
- `controls-elmish-reference`: required — generated app references Controls.Elmish
- `persistent-host-wiring`: required — generated app wires the persistent viewer host
- `no-bounded-only-default`: required — no bounded-only/print-only default launch path
- `claude-skill-peers`: required — .claude skill peers present for every .agents skill
- `no-framework-source`: required — no copied framework implementation/source/specs

Contract changelog:
- 1.0 `(baseline)` added: Structural contract codified with an explicit schema version and deprecation window (feature 046).
