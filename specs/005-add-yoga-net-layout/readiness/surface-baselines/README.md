# Surface Baselines

Run `dotnet test tests/Package.Tests/Package.Tests.fsproj --filter "Surface baselines"` after refreshing the `FS.Skia.UI.Layout` baseline for this feature. The final `FS.Skia.UI.Layout.txt` artifact records the exported public names from the built package.
