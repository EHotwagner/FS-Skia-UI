# Surface baseline shape (feature 116, T006)

The intended public + per-package surface delta this feature introduces. The authoritative regeneration
is `RefreshSurfaceBaselines` (T024); this note records the expected shape so the diff is reviewable.

## Top-level public surface (additive)

- `FS.Skia.UI.Controls.Elmish.FrameMetrics` gains six public fields:
  `RepaintedNodeCount: int`, `DirtyRectCount: int`, `DirtyArea: int`,
  `PictureCacheHitCount: int`, `PictureCacheMissCount: int`, `PictureCacheEntryCount: int`.
- `FS.Skia.UI.Controls.ControlDiagnosticCode` gains one advisory case: `OffscreenComposition`.

## Per-package surface

- `readiness/per-package-surface/FS.Skia.UI.Controls.Elmish.fsi.txt` — the six `FrameMetrics` fields
  (+12 lines).
- `readiness/per-package-surface/FS.Skia.UI.Controls.fsi.txt` — the `OffscreenComposition` diagnostic
  case + the internal `RetainedRender` additions visible on the internal surface
  (`PictureCacheKey` / `PictureCache` types, `WorkReductionRecord` carriers, `PictureCache` /
  `PictureCacheEnabled` fields, `PictureCacheCap` / `offscreenEffect` vals) (+30 lines).

## Template api-surface doc

- `template/base/docs/api-surface/Controls/Types.fsi` — the additive `OffscreenComposition` case
  (regenerated by `RefreshSurfaceBaselines`; checked by `TemplateDrift`).

No existing public signature changed shape; the delta is purely additive (six fields + one DU case). The
internal `RetainedRender` damage-set / picture-cache / always-miss-flag additions stay internal (reached
via `InternalsVisibleTo`).
