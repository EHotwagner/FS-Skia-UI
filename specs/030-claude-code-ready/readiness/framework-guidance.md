# Framework Guidance

Claude users should start from `CLAUDE.md`, which imports `AGENTS.md`. Active feature details remain in the Spec Kit plan named by `AGENTS.md`.

Project skills in `.claude/skills/` are the canonical Claude Code workflow surface. Command aliases in `.claude/commands/` are optional compatibility wrappers.
