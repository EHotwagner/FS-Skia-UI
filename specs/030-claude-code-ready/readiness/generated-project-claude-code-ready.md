# Generated Project Claude Code Ready

Generated product validation exercises the same production generation paths used by source and package template checks:

- `TemplateCheck` validates dotnet template source/package output.
- `GeneratedProductCheck` copies selected Codex skills and matching Claude skills through `copySelectedSkills`.
- Generated file-list reports include `Selected skills` and `Selected Claude skills` sections.

No generated project requires `.claude/settings.local.json` or user-level `~/.claude` files.
