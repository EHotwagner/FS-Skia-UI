# Capability Loading Workflow

Runtime FS.Skia.UI capability skills are valid-empty for this feature because no product runtime behavior or public capability API is changed.

Tasks with declared capability skills load the listed skill before edits and record that in `readiness/skill-loading-evidence.md`.
