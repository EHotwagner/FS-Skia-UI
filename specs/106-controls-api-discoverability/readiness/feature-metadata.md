# Feature metadata record (T004)

- **Feature**: 106-controls-api-discoverability (active per `.specify/feature.json`).
- **Tier**: 1 (contracted change). Public Controls `///` documentation (carried in `.fsi`)
  changes, the sample/template contract (`View.fs`, `README.md`, bundled catalog reference)
  changes, the api-surface bundle is regenerated, and a new governance gate
  (`ControlsDocCoverageCheck`) is added and routed.
- **Affected layer**:
  - `src/Controls/**/*.fsi` — `///` doc-only edits across the public surface (no signature
    shape change). **Comprehensive scope** (maintainer decision 2026-06-12): all three
    placeholder wordings (`Public contract function|type|module exposed …`) were removed, not
    only the `function` variant — 356 placeholder lines across ~30 files, plus the `Empty`
    rule sweeping the rest of the surface.
  - `template/base/src/Product/View.fs` — rewritten to the typed Props front door
    (`FS.Skia.UI.Controls.Typed`).
  - `template/base/README.md`, `template/base/docs/controls-catalog.md` — discovery pointer +
    bundled per-control catalog reference.
  - `build/Governance/**` — the new gate (`ControlsDocCoverage.fs`/`.fsi`) + wiring
    (`Targets.fs`/`.fsi`, `Routing.fs`, `Front/Helpers.fs`, `Engine/Update.fs`),
    `validation.contract.yml` regenerated by `RefreshSurfaceBaselines`.
- **Public-API impact**: **No `.fsi` signature *shape* change** (no added/removed/retyped
  `val`/`type`/`member`). `///` docs added/replaced only. Per-package surface baselines are
  byte-stable (`PerPackageSurface.normalize` strips `//`-prefixed lines); the api-surface
  *bundle* (`template/base/docs/api-surface/Controls/*.fsi`) is regenerated current. A new
  `ControlsDocCoverageCheck` routable gate is added (derived into
  `AgentValidation.knownGates` via `Targets.routableGates`).
- **Elmish/MVU applicability**: **N/A** — the feature adds no stateful/I/O product workflow.
  The gate is a pure `.fsi`-text → findings analysis (`ControlsDocCoverage.analyze`); file
  reads + report write stay at the `Engine/Interpret.fs`/`Update.fs` edge, matching the
  `DesignTokenDrift` precedent. No product `Model`/`Msg`/`Effect` introduced.
- **Evidence obligations**: (1) `ControlsDocCoverageCheck` green over the real surface
  (`findings=0`, `readiness/doc-coverage.md`); (2) the api-surface bundle current + per-package
  baseline stable (`readiness/surface-baselines.md`); (3) `GeneratedProductCheck` — the
  rewritten starter compiles + renders (`readiness/generated-product.md`); (4) `TemplateCheck`
  — bundled catalog reference + README pointer present (`readiness/template-check.md`);
  (5) zero `.fsi` shape delta (`readiness/zero-surface-delta.md`); (6) `EvidenceGraph` +
  `EvidenceAudit` verdict PASS with 0 synthetic.
- **Synthetic evidence**: none. The gate runs against the real `src/Controls/**/*.fsi`
  surface; the starter migration is proven by the real `TypedLoweringTests` parity suite (with
  RichText/LineChart/GraphView cases added in T008); `GeneratedProductCheck` renders the real
  product. No `[S]`/`[SEH]` tasks.
