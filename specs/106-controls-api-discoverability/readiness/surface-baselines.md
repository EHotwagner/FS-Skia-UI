# Surface-baseline currency + stability (T015, FR-004/SC-006)

## Per-package baseline byte-stable (doc-only `.fsi` edits)

```
$ git status --short readiness/per-package-surface/
(no FS.Skia.UI.Controls.fsi.txt change)
```

Adding/replacing `///` comments on the Controls `.fsi` does **not** churn the per-package
surface baseline (`readiness/per-package-surface/FS.Skia.UI.Controls.fsi.txt`), because
`PerPackageSurface.normalize` strips `//`-prefixed lines (which includes `///`) before diffing.
No signature shape changed, so the baseline is byte-identical.

## api-surface bundle regenerated current (FR-004/SC-006)

```
$ git status --short template/base/docs/api-surface/Controls/ | wc -l
28   (regenerated by ./fake.sh build -t RefreshSurfaceBaselines)
```

The consumer-visible api-surface bundle (`template/base/docs/api-surface/Controls/*.fsi`)
copies `.fsi` verbatim **with** comments, so it changed and was regenerated by
`RefreshSurfaceBaselines`; `ApiSurfaceGen.currency` passes. The typed `Widgets/*.fsi`
contracts the starter relies on are present in the bundle (feature 089 enrollment confirmed to
hold), now carrying substantive `///` docs (0 placeholder lines):

```
$ grep -c "Public contract" template/base/docs/api-surface/Controls/Catalog.fsi
0
```

So the documented typed front door is present in the generated project's reference and cannot
silently drift out of it.
