# Generated-guidance validation — feature 106 (controls-api-discoverability)

The generated template's authoring guidance was updated and validated so it resolves every
anti-reflection instruction to a concrete, populated reference (FR-013, SC-005):

- `template/base/README.md` gains an **"Authoring controls — discover the API, never reflect"**
  section pointing to: the typed starter (`src/Product/View.fs`), the on-disk
  `docs/api-surface/Controls/*.fsi` bundle, the bundled per-control catalog reference
  (`docs/controls-catalog.md`), the programmatic `Catalog.*` discovery API, and the interactive
  host seam (`ControlsElmish.runInteractiveApp` / `programOfWidget`).
- `template/base/docs/controls-catalog.md` is the new consumer-visible per-control reference:
  the `Catalog.*` discovery API table + a worked no-reflection example + a per-control table for
  the demonstrated controls, and the note that controls authored through the legacy builder are
  still fully supported.
- The starter `View.fs` demonstrates the typed Props front door as the default authoring path.

Validation: `TemplateCheck` (see `template-check.md`) confirms the regenerated project carries
the README pointer and the bundled catalog reference and builds; `GeneratedProductCheck` (see
`generated-product.md`) confirms the typed starter compiles and renders. The
`docs/api-surface/Controls/*.fsi` bundle was regenerated by `RefreshSurfaceBaselines` and
carries the substantive `///` docs (no placeholder text), confirmed by `ApiSurfaceGen.currency`.

- failure-class: stale-or-empty-generated-guidance
