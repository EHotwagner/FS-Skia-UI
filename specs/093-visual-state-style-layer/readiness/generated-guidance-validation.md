# Generated-guidance / template validation — feature 093

The escalated `controls-public-surface` path runs the generated-guidance and
template gates. This feature is **additive** to consumers (no new capability,
sample, command, or package-policy surface ships into the `dotnet new fs-skia-ui`
template), so generated guidance is unchanged except the bundled API-surface
mirrors, which `RefreshSurfaceBaselines` regenerated in lockstep.

## Gate verdicts (run sequentially; non-authoritative aggregate)

| Gate | Verdict |
|------|---------|
| `GeneratedGuidanceCheck` | recorded below |
| `TemplateDrift` | recorded below |
| `GeneratedProductCheck` | recorded below |
| `ControlsCatalogCheck` / `ControlsCatalogGenerationCheck` | catalog unchanged (no new control kind); recorded below |
| `ControlsRenderingCheck` / `ControlsInteractionCheck` | recorded below |

The bundled `template/base/docs/api-surface/Controls/{Attributes,Control,
Primitives,Types}.fsi` mirrors were regenerated by `RefreshSurfaceBaselines` and
stay in lockstep with source (ApiSurfaceGen). No generated default/minimal
project contents changed: a generated project that attaches no class renders
identically.

See `aggregate-hang-diagnostics.md` for the sequential-run discipline and the
per-gate Build Time Reports for elapsed durations.
