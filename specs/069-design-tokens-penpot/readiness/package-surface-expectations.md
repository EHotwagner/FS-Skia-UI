# FS.Skia.UI.Controls surface delta (feature 069)

- **Authoritative command**: `./fake.sh build -t PackageSurfaceCheck` (and
  `PerPackageSurfaceDiff`)
- **Baselines**: `readiness/per-package-surface/FS.Skia.UI.Controls.fsi.txt` and
  `readiness/surface-baselines/FS.Skia.UI.Controls.txt`
- **Failure class**: `non-additive-surface`

## Expected delta (additive-only — SC-008)

The only public-surface change is the **new** `DesignTokens` module:

```
module DesignTokens
  module Light
    val foreground : Color
    val background : Color
    val accent : Color
    val danger : Color
    val muted : Color
    val fontFamily : string option
    val fontSize : float
    val density : float
    val cornerRadius : float
    val contrastRequiredRatio : float
  module Dark
    val foreground : Color
    val background : Color
    val accent : Color
    val danger : Color
    val muted : Color
    val fontFamily : string option
    val fontSize : float
    val density : float
    val cornerRadius : float
    val contrastRequiredRatio : float
```

- The `Theme` type and `Theme` module signatures (`Types.fsi`, `Theme.fsi`) are
  **unchanged**.
- `Control.fsi` is unchanged.
- No existing names are removed or retyped — the delta is purely the new
  `DesignTokens` names, so both per-package and aggregate baselines change
  additively.

## Realized delta (additive-only — confirmed)

After `DesignTokens.fsi`/`.fs` landed and `Theme.fs` was re-expressed:

- **Aggregate baseline** `readiness/surface-baselines/FS.Skia.UI.Controls.txt`
  (regenerated by `RefreshSurfaceBaselines`) — added exactly three entries, no
  removals:
  ```
  +FS.Skia.UI.Controls.DesignTokens
  +FS.Skia.UI.Controls.DesignTokens+Dark
  +FS.Skia.UI.Controls.DesignTokens+Light
  ```
- **Per-package snapshot** `readiness/per-package-surface/FS.Skia.UI.Controls.fsi.txt`
  (regenerated via `PerPackageSurface.captureCurrent`) — added the
  `module DesignTokens` block (`Light`/`Dark` × 10 vals) only; **zero removed lines**.

`Types.fsi`, `Theme.fsi`, and `Control.fsi` are unchanged. The delta is purely the new
`DesignTokens` names — SC-008 satisfied. `PackageSurfaceCheck` / `PerPackageSurfaceDiff`
confirm additive-only.

## Typed token surface (US3)

`DesignTokens.fsi` is the curated, greppable token surface. A consumer references a token
by typed name (`DesignTokens.Light.accent`) and it resolves to the DTCG value — exercised
by the FSI transcript (`readiness/fsi/design-tokens-surface.txt`) and the T024 test in
`tests/Controls.Tests/DesignTokenParityTests.fs`.
