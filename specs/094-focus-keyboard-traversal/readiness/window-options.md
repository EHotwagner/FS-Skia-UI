# Window Options (094)

status=not-applicable

## Option rows

- option=resize — resize=not-applicable (no window created)
- option=maximize — maximize=not-applicable (no window created)
- option=startup-state — startup-state=not-applicable (no window created)
- option=startup-position — startup-position=not-applicable (no window created)
- option=backend — backend=not-applicable (no windowed backend selected; the focus proofs run
  through pure reducers, the offscreen `routeFocusedKey` adapter path, and the off-window
  `Control.renderTree` responds-proof)

No unsupported window setting is requested, and no option failure is hidden under another
diagnostic class. This feature opens no window, so there are no window options to honor or degrade.
