# Validation contract currency (FR-007/FR-011, SC-007)

The new distribution targets and routing rule are wired into governance and the generated
`validation.contract.yml` was regenerated from the compiled `Routing.fs` single source via
`./fake.sh build -t RefreshSurfaceBaselines`, so the currency checks cannot drift.

## Wiring

- **Targets** (`build/Governance/Targets.fs`): `PrePublishCheck` (prereq `TemplateCheck`) and
  `Publish` (prereqs `PrePublishCheck; PackLocal; TemplatePack`) added to the DU, `name`,
  `directPrerequisites`, and `allTargets` (registry 38 → 40).
- **knownGates** (`AgentValidation.fs`): `Publish` and `PrePublishCheck` added to the allowlist.
- **Routing** (`build/Governance/Routing.fs`): new `distribution` rule (maintainer-verify)
  matching the publish/pre-publish validator sources + `template/base/build.fsx` +
  `Directory.Packages.props` + `docs/UPGRADING.md`, requiring `PrePublishCheck`, `TemplateCheck`,
  `GeneratedProductCheck`, `TargetMetadataDrift`, `EvidenceGraph`, `EvidenceAudit`.
- **validation.contract.yml**: regenerated; now renders the `distribution` rule and the two new
  targets.

## Currency gates (all PASS)

```
./fake.sh build -t TargetMetadataDrift    -> Status: Ok   (validation.contract.yml current vs Routing.fs; registry 40 rows drift-free)
./fake.sh build -t SkillSyncCheck         -> Status: Ok   (.claude skill tree current vs .agents)
./fake.sh build -t GeneratedGuidanceCheck -> Status: Ok   (generated guidance current)
```

The `.claude` skill tree, `docs/evidence-formats.md`, `docs/api-surface/**`, and
`docs/skillist-reference.md` regenerated byte-identically (no governance drift). SC-007:
all currency gates green with the new targets in `knownGates` / `validation.contract.yml`.
