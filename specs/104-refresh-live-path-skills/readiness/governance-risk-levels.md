# Governance risk levels — feature 104 (live-path skill currency, T002/T022)

Feature 104 is a **Tier-2 (internal/documentation)** change: a pure documentation-currency
(skill-honesty) pass that refreshes three Markdown skill files so their live-path disposition is
current through feature 103. It makes **no `.fsi` signature change** (public or internal), no logic
change, and no behavior change — every edit is skill-documentation Markdown. There is **zero surface
delta** (SC-005), so no per-package or cross-package baseline moves.

`./fake.sh build -t Route` printed `tier=agent-ready`,
`matched-rules=evidence-governance, specify-catchall, docs-only, template-docs, skill-quality`, and
the 10-gate set below. It did **not** escalate to the `controls-public-surface` set: that escalation
fires on `src/Controls/**/*.fs(i)` edits, and feature 104's only `src/Controls/**` change is
`src/Controls/skill/SKILL.md` (Markdown), which routes via `skill-quality` / `docs-only`. `Route` is
authoritative; the gate set below is what was run, **sequentially** (shared `.fake` state, never
concurrently). `git diff --stat` shows **zero** `src/**/*.fsi` and **zero** `src/**/*.fs` lines — the
only `src/**` file touched is `src/Controls/skill/SKILL.md` — and no product test moved (SC-004/SC-005).
`RefreshSurfaceBaselines` regenerated the per-package surface baselines **byte-identical** (no drift).

## small

The `.agents/skills/fs-skia-reconciliation` refresh (US1) and the NEW
`.agents/skills/fs-skia-controls-host` skill (US3) — `.agents`-domain Markdown, routed to the skill
governance gates (`SkillQualityCheck`, `SkillSyncCheck`).
- required evidence: the refreshed/new skill bodies satisfy all 7 `SkillQualityCheck` rubric
  sections; the `.claude/skills/**` mirror is regenerated byte-identical (`RefreshSurfaceBaselines`,
  T019); the new `fs-skia-controls-host` id appears in the regenerated `skillist-reference.md`.
- gate: `SkillQualityCheck`, `SkillSyncCheck`.

## medium

The `src/Controls/skill/SKILL.md` edit (US2) — E3 names the public `deriveVisualState` / internal
`applyRuntimeVisualState` surface (096); E4 describes `Focus.route`'s post-100 `role`+`navRange`→
`NavIntent` shape (100).
- required evidence: both edits stay within the existing E3/E4 headings; no code example references a
  superseded signature; the skill still passes all 7 rubric sections.
- gate: `SkillQualityCheck` (+ the controls-public-surface escalation below if Route prints it).

## broad

Per the feature 101/102 precedent, **any** edit under `src/Controls/**` — even a pure-Markdown skill
edit with zero `.fsi` delta — can escalate `Route` to the controls-public-surface gate set. US2 edits
`src/Controls/skill/SKILL.md`, so escalation is expected. The public surface and the per-package
internal `.fsi.txt` baselines are **unchanged** (no `.fsi` move).
- required evidence: `git diff --stat` shows **zero** `src/**/*.fsi` lines and the only `src/**` file
  touched is `src/Controls/skill/SKILL.md` (Markdown); no product test file changed (SC-004/SC-005);
  `EvidenceGraph` + `EvidenceAudit verdict=PASS` with **0 synthetic**.
- broad validation: the `Route`-printed gate set, run **sequentially** (shared `.fake` state) in
  deterministic order; aggregate results are recorded as **non-authoritative** unless re-confirmed
  sequentially (see `aggregate-hang-diagnostics.md`).

authoritative-gate-list=Dev, GeneratedGuidanceCheck, SkillSyncCheck, SkillQualityCheck, PhaseHookParityCheck, SkillContractPathCheck, TemplateUpdateSkillPackageCheck, TemplateDrift, EvidenceGraph, EvidenceAudit

All ten gates passed sequentially. `Route --enforce` named no missing evidence artifact, so no broad
validation beyond the printed set was required. The `Dev` aggregate passed in 2m42s (Restore 33.6s,
Build 46.2s, SampleContractSmoke 8.5s, Test 1m13.9s); it is recorded as a **non-authoritative**
aggregate result — the per-suite Expecto outcomes are the authoritative evidence (see
`aggregate-hang-diagnostics.md`). No `GeneratedProductCheck` ran (not in the Route set); no
environment-class failure was observed.
