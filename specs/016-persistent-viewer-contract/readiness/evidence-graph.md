# Evidence Graph Evidence

PASS: `EvidenceGraph` refreshed `task-graph.md` and `task-graph.json`.

Persistent viewer evidence expectations:

- persistent graphical launch artifacts
- bounded smoke helpers
- first-frame helpers
- frame-count helpers
- `[S*]` propagation visibility
- Supported-host persistent launch evidence cannot be synthetic
